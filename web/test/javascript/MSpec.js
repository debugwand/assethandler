describe("prevent jasmine aborting the task", function () {
    it ("is an empty test otherwise jasmine will throw its toys out the pram", function () {
        
    });
});

/*
so jasmine catastrophically fails the task if it cannot find at least one test
can be solved with grunt --force, but I'm not keen on it because it affects all tasks.
 */